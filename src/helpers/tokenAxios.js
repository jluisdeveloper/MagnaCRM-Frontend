export default function setAuthToken(axios, token) {
  // axios.inrceptors.request.use(config => {
  //   config.headers.Authorization = token;
  //   return config;
  // }), error => {
  //   return Promise.reject(error);
  // }
  // axios.defaults.headers.common['Authorization'] = token
}